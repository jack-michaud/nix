[project]
name = "claude-rlm"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
claude_rlm = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/claude_rlm"]
