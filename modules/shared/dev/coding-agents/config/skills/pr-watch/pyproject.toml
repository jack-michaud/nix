[project]
name = "pr-watch"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pr_watch = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pr_watch"]
