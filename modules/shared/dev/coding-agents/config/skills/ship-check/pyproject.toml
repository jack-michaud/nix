[project]
name = "ship-check"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ship_check = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ship_check"]
