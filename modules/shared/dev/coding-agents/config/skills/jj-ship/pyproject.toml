[project]
name = "jj-ship"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
jj_ship = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jj_ship"]
