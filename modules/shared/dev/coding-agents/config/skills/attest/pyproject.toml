[project]
name = "attest"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["httpx"]

[project.scripts]
attest = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/attest"]
