[project]
name = "peer-bus"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
peer_bus = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/peer_bus"]
