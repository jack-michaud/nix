[project]
name = "nix-config-change"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
nix_config_change = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nix_config_change"]
